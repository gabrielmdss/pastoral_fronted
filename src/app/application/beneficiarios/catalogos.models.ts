export interface GrupoDistribuicao {
  id: string;
  codigo: string;
  nome: string;
  regraCalendario: string;
}
export interface MotivoDesligamento {
  id: string;
  codigo: string;
  descricao: string;
}
