import { TestBed } from '@angular/core/testing';import { Router } from '@angular/router';import { describe,expect,it,vi } from 'vitest';import { authGuard } from './auth.guard';import { permissionGuard } from './permission.guard';import { SessionFacade } from './session.facade';
describe('guards',()=>{it('AuthGuard redireciona sessão ausente',()=>{TestBed.configureTestingModule({providers:[{provide:SessionFacade,useValue:{isAuthenticated:()=>false}},{provide:Router,useValue:{createUrlTree:vi.fn(()=> 'login')}}]});expect(TestBed.runInInjectionContext(()=>authGuard({} as never,{} as never))).toBe('login');});it('PermissionGuard permite permissão efetiva de /me',()=>{TestBed.configureTestingModule({providers:[{provide:SessionFacade,useValue:{hasPermission:(p:string)=>p==='ESTOQUE_VISUALIZAR'}},{provide:Router,useValue:{createUrlTree:vi.fn()}}]});expect(TestBed.runInInjectionContext(()=>permissionGuard('ESTOQUE_VISUALIZAR')({} as never,{} as never))).toBe(true);});});
