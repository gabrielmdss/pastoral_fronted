import { describe,expect,it } from 'vitest';import { mapDashboard } from './dashboard-api.mapper';import type { Dashboard } from '../../../application/dashboard/dashboard.model';
describe('Dashboard API mapper',()=>{it('preserva o read model sem criar regra de negócio',()=>{const data={beneficiarios:{capacidade:1,ativos:1,vagas:0,excedente:0,listaEspera:0},proximaDistribuicao:null,saude:{proximaDistribuicao:{demanda:0,capacidadeGarantida:0,coberturaPercentual:100,deficitCestas:0,status:'SEM_PLANEJAMENTO'},competenciaAtual:{demanda:0,capacidadeGarantida:0,coberturaPercentual:100,deficitCestas:0,status:'SEM_PLANEJAMENTO'},competenciaSeguinte:null},competenciaAtual:null,estoque:{insumosAtivos:0,itensComEstoque:0,itensCriticos:0,cestasMontadas:0,cestasReservadasParaDistribuicoes:0},insumosCriticos:[],alertas:[]} as Dashboard;expect(mapDashboard({data})).toEqual(data);});});
