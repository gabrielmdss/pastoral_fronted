export interface CompetenciaDto {
  id: string; ano: number; mes: number; status: string; distribuicoes: number; direitos: number;
}
export interface GerarCompetenciaRequestDto { ano: number; mes: number; }

