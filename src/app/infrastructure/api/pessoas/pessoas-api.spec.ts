import{provideHttpClient}from'@angular/common/http';import{provideHttpClientTesting,HttpTestingController}from'@angular/common/http/testing';import{TestBed}from'@angular/core/testing';import{afterEach,beforeEach,describe,expect,it}from'vitest';import{APP_CONFIG}from'../../config/app-config';import{PessoasApiService}from'./pessoas-api.service';describe('PessoasApiService',()=>{let api:PessoasApiService,http:HttpTestingController;beforeEach(()=>{TestBed.configureTestingModule({providers:[provideHttpClient(),provideHttpClientTesting(),PessoasApiService,{provide:APP_CONFIG,useValue:{apiBaseUrl:'http://api/v1'}}]});api=TestBed.inject(PessoasApiService);http=TestBed.inject(HttpTestingController);});afterEach(()=>http.verify());it('busca por nome e documento',()=>{api.buscar({q:'Maria'}).subscribe();expect(http.expectOne(r=>r.params.get('q')==='Maria').request.method).toBe('GET');api.buscar({documento:'123'}).subscribe();expect(http.expectOne(r=>r.params.get('documento')==='123').request.method).toBe('GET');});it('cadastra CPF e RG semanticamente e obtém detalhe',()=>{api.criar({nomeCompleto:'Maria',dataNascimento:null,telefone:null,documentos:[{tipo:'CPF',numero:'123',principal:true},{tipo:'RG',numero:'456',principal:false}]}).subscribe();const post=http.expectOne('http://api/v1/pessoas');expect(post.request.body.documentos.map((x:{tipo:string})=>x.tipo)).toEqual(['CPF','RG']);post.flush({data:{id:'9'}});api.obter('9').subscribe();expect(http.expectOne('http://api/v1/pessoas/9').request.method).toBe('GET');});});
