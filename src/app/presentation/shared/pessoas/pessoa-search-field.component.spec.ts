import{TestBed}from'@angular/core/testing';import{of,Subject}from'rxjs';import{afterEach,describe,expect,it,vi}from'vitest';import{BuscarPessoasUseCase,CriarPessoaUseCase}from'../../../application/pessoas/pessoas.use-cases';import{PessoaSearchFieldComponent}from'./pessoa-search-field.component';const pessoa={id:'1',nomeCompleto:'Maria',dataNascimento:null,documentos:[{tipo:'CPF',numeroMascarado:'***42'}],beneficiario:null};describe('PessoaSearchField',()=>{afterEach(()=>vi.useRealTimers());it('aplica debounce, escolhe filtro e cancela busca anterior',async()=>{vi.useFakeTimers();const first=new Subject<typeof pessoa[]>(),second=new Subject<typeof pessoa[]>(),execute=vi.fn().mockReturnValueOnce(first).mockReturnValueOnce(second);TestBed.configureTestingModule({imports:[PessoaSearchFieldComponent],providers:[{provide:BuscarPessoasUseCase,useValue:{execute}},{provide:CriarPessoaUseCase,useValue:{}}]});const page=TestBed.createComponent(PessoaSearchFieldComponent).componentInstance;page.search.setValue('Maria');await vi.advanceTimersByTimeAsync(351);expect(execute).toHaveBeenCalledWith({q:'Maria'});page.search.setValue('12345');await vi.advanceTimersByTimeAsync(351);expect(execute).toHaveBeenLastCalledWith({documento:'12345'});expect(first.observed).toBe(false);});it('seleciona resultado e pessoa recém-criada',async()=>{TestBed.configureTestingModule({imports:[PessoaSearchFieldComponent],providers:[{provide:BuscarPessoasUseCase,useValue:{execute:()=>of([pessoa])}},{provide:CriarPessoaUseCase,useValue:{execute:()=>of(pessoa)}}]});const page=TestBed.createComponent(PessoaSearchFieldComponent).componentInstance;page.select(pessoa);expect(page.selected()).toEqual(pessoa);page.form.setValue({nomeCompleto:'Maria',dataNascimento:null,telefone:null,tipo:'RG',numero:'123'});await page.create();expect(page.selected()).toEqual(pessoa);});});
