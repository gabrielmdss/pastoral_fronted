# Testes

Os testes cobrem use case de login, facade de sessão, interceptador JWT, guards, mappers, páginas e os três requests HTTP. Execute `npm test -- --watch=false`. Lint, typecheck e build são gates separados. Teste manual real requer backend/banco ativos e credenciais fornecidas fora do repositório; nenhuma senha deve ser versionada.
