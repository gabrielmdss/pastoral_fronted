# Usado por scripts/write-runtime-config.mjs antes do build (npm run build:homolog /
# build:producao). Não é lido pelo Angular em runtime — só serve para gerar
# public/config.json antes do `ng build`. Em Vercel/Render, defina como variável
# de ambiente do projeto em vez de commitar um arquivo .env real.
API_BASE_URL=https://api-homolog.exemplo.org/api/v1
